{
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@types/node": "^20.3.3"
  },
  "scripts": {
    "build": "npx tsc",
    "start": "node build/app.js",
    "server": "npm run build && npm run start"
  }
}
